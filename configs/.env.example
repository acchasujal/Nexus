# Environment Variables — Template
# Copy this file to .env and fill in values. NEVER commit .env to git.

# ── Backend ────────────────────────────────────────────────────────────────────
DATABASE_URL=                    # Catalyst Data Store connection string
JWT_SECRET_KEY=                  # Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# ── AI / Copilot ───────────────────────────────────────────────────────────────
# Fill in after M1 spike verifies QuickML behavior
QUICKML_API_KEY=
QUICKML_ENDPOINT=

# ── Catalyst ───────────────────────────────────────────────────────────────────
CATALYST_PROJECT_ID=
CASECLOCK_REPOSITORY=local       # local | catalyst
CATALYST_AUTH=                   # JSON OAuth refresh credential; never commit real value
CATALYST_OPTIONS=                # JSON: {"project_id":"","project_key":"","api_domain":"https://api.catalyst.zoho.in","accounts_domain":"https://accounts.zoho.in","environment":"Development"}

# ── App ────────────────────────────────────────────────────────────────────────
ENVIRONMENT=development          # development | staging | production
LOG_LEVEL=INFO
FRONTEND_URL=http://localhost:5173
